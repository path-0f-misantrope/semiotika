# Semiotika

**Semiotika** — это сервис для автоматического распознавания текста песен. Приложение разделяет аудиофайл на дорожки (выделяет вокал) с помощью **Demucs**, а затем транскрибирует текст используя нейросеть **Whisper**.

## 🚀 Функциональность

*   **Регистрация и авторизация** пользователей.
*   **Загрузка аудиофайлов** через веб-интерфейс.
*   **Автоматическое отделение вокала** от музыки (используется Demucs).
*   **Распознавание текста** (Speech-to-Text) с поддержкой русского языка (используется OpenAI Whisper).
*   **Личный кабинет** (Dashboard) с историей загруженных песен и их текстами.

## 🛠 Технологический стек

Проект построен на микросервисной архитектуре:

*   **Backend**: Go (Golang) + Gin Framework. Отвечает за веб-сервер, маршрутизацию, авторизацию и оркестрацию процессов.
*   **Database**: PostgreSQL. Хранит данные пользователей и тексты песен.
*   **Vocal Separation**: Demucs (запускается в Docker-контейнере).
*   **Transcriber**: Python + Flask + OpenAI Whisper. Отдельный сервис для обработки аудио в текст.
*   **Frontend**: HTML-шаблоны (Server-Side Rendering через Gin).

## ⚙️ Установка и запуск

### Предварительные требования
*   Go 1.20+
*   Python 3.10+
*   Docker (с поддержкой NVIDIA GPU рекомендуется для скорости)
*   PostgreSQL
*   FFmpeg (требуется для обработки аудио)

### 1. Настройка базы данных
Убедитесь, что у вас запущен PostgreSQL.
В проекте используется строка подключения:
`postgres://postgres:1234@localhost:8080/postgres`

Вам необходимо создать базу данных `postgres` и таблицы `users` и `songs`.
> *Примечание: В коде используется `database.DbInit`, который, вероятно, создает таблицы автоматически, но убедитесь, что база данных существует.*

### 2. Сборка Docker образа для Demucs
Для работы разделения звука необходимо собрать Docker-образ:

```bash
docker build -t my-demucs -f Dockerfile.demucs .
```

### 3. Запуск Python сервиса (Whisper)
Установите зависимости (рекомендуется использовать виртуальное окружение):

```bash
pip install torch torchvision torchaudio openai-whisper flask
```

Запустите сервис транскрибации:

```bash
python whhhusper.py
```
*Сервис запустится на порту 5000.*

### 4. Запуск Go сервера
Установите зависимости и запустите основное приложение:

```bash
go mod tidy
go run main.go
```
*Сервер запустится на порту 8787.*

## ⚠️ Важное примечание по конфигурации

В текущей версии проекта (`main.go`) используются **хардкодные абсолютные пути**, специфичные для среды разработки (Windows):
*   `D:/poznovatel/audio/...`

**Перед запуском убедитесь, что вы изменили эти пути в `main.go` на актуальные для вашей системы**, либо создайте соответствующую структуру папок.

## 📂 Структура проекта

*   `main.go` — Основной сервер приложения.
*   `whhhusper.py` — Микросервис для Whisper.
*   `Dockerfile.demucs` — Конфигурация окружения для Demucs.
*   `templates/` — HTML шаблоны (login, register, index, dashboard).
*   `database.go` — Логика работы с БД.

